import { ToolPlaceholderPage } from '@/components/tool-placeholder-page';

export default function AnnotatePage() {
  return (
    <ToolPlaceholderPage
      title="Annotate PDF"
      description="Add text notes, highlight, and redact content in your PDF documents."
    />
  );
}
